#include <iostream>
#include <cmath>
using namespace std;

int main(){

    int base, exponent, power;
    cin >> base >> exponent;

    power = pow(base, exponent);
    cout << power ; 

}